{
  "name": "@ilms/server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/bin.ts",
    "start": "bun run src/bin.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepare:playwright": "bunx -p playwright@1.49.0 playwright install chromium"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.4.0",
    "@ilms/contracts": "workspace:*",
    "@ilms/shared": "workspace:*",
    "elysia": "^1.4.28",
    "playwright-core": "^1.49.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.9",
    "typescript": "^5.7.3"
  }
}
